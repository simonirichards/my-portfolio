version: 0.1

phases:
  pre_build:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install
      - npm test
  build:
    commands:
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/*
    - images/*
    - dist/bundle.js